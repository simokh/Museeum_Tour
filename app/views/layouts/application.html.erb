<!DOCTYPE html>
<html>
  <head>
    <title>Museum Tour</title>
    <%= csrf_meta_tags %>
    <%
=begin%>
 <%= csp_meta_tag %> 
<%
=end%>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  
  <header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
  <div class="my-nav">
      <div class="container">
        <ul>
          <li><%= link_to "Home", root_path %></li>
          <li><%= link_to "Museums", museums_path %></li>
          <li><%= link_to "Add a Museum", new_museum_path if current_user %></li>
          <% if current_user %>
            <li class="li-right"><span class="user"><%= link_to "#{current_user.user_name.capitalize} (#{current_user.reviews.count})", user_path(current_user) %></span> | <%= link_to "Sign out", logout_path %></li>
          <% else %>
            <li class="li-right"><%= link_to "Sign up", signup_path %></li>
            <li class="li-right"><%= link_to "Log in", login_path %></li>
          <% end %>
        </ul>
      </div>
    </div>
    <div class="content-wrapper">
      <div class="container">
        <p id="notice"><%= notice %></p>
        <p id="alert"><%= alert %></p>
</header>
    <%= yield %>
    </div>
    <div class="my-footer">
      Museums Reviews &copy; <%= Date.today.year %>
    </div>
  </body>
</html>
